const javascriptExamples = [
  `function printTriangle() {
\tfor (let i = 0; i < 5; i++) {
\t\tlet line = '';
\t\tfor (let j = 0; j < i + 1; j++) {
\t\t\tline += '*';
\t\t}
\t\tconsole.log(line);
\t}
}

printTriangle();`,
  // 다른 자바 예제들 추가
];

export default javascriptExamples;
