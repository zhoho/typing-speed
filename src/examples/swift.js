const swiftExamples = [
  `func printTriangle() {
\tfor i in 0..<5 {
\t\tvar line = ""
\t\tfor _ in 0..<i + 1 {
\t\t\tline += "*"
\t\t}
\t\tprint(line)
\t}
}

printTriangle()`,
  // 다른 자바 예제들 추가
];

export default swiftExamples;
